---
name: db-migration
description: Database migration workflow for this project, including schema updates, test-schema generation, migration file creation, and applying migrations. Use when asked to change database schema or create a migration.
---

# Database Migration

1. Update `database/app` and `database/seed` as needed.
   - Do not edit autogenerated files like `test-schema.sql`.

2. Validate schema:
```bash
make generate-test-schema
```

3. **Find the next migration number** by checking the latest existing migration:
```bash
ls database/migrate/ | sort -n | tail -1
```
This will show something like `397_backfill_group_connections.sql` — the next migration would be `398_your_desc.sql`.

4. Create and apply migration:
- Add a new file in `database/migrate/` named `{next_number}_{desc}.sql` (incrementing from the number found in step 3).
- **IMPORTANT: Never put MV (materialized view) definitions in migrations.** MVs are JIT compiled from `server/app/sql/views/*.sql` by `make sql-compile`. Migrations should only contain DDL for tables, indexes, constraints, and enum values. To change an MV, edit its source SQL file directly.
- Run:
```bash
make migrate-db
```

5. Inspect database (if needed):
```bash
psql postgresql://myuser:mypassword@localhost:5432/mydb
```

Credentials:
```bash
DB_USER="myuser"
DB_PASSWORD="mypassword"
DB_NAME="mydb"
DB_PORT="5432"
DB_HOST="localhost"
```
