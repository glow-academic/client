# =============================================================================
# University Configuration — full university setup (organization + university)
# =============================================================================
modules:
  providers: all
  models: all
  agents: all
  tools: all
  auth: all
  rubrics: all
  evals: all
  profiles: all
  settings: all

  setup: university

  organization:
    departments: all
    profiles: all
    settings: all

  university:
    departments: all
    personas: all
    documents: all
    uploads: all
    texts: all
    fields: all
    parameters: all
    rubrics: all
    simulations: all
    scenarios: all
    cohorts: all
    profiles: all
    settings: all
