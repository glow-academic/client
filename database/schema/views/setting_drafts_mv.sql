-- Materialized view: setting_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: setting_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.setting_drafts_mv AS
 WITH draft_links AS (
         SELECT setting_drafts_agents_connection.draft_id,
            'agents'::text AS resource_type,
            setting_drafts_agents_connection.agents_id AS resource_id
           FROM public.setting_drafts_agents_connection
          WHERE (setting_drafts_agents_connection.active = true)
        UNION ALL
         SELECT setting_drafts_auth_item_keys_connection.draft_id,
            'auth_item_keys'::text AS resource_type,
            setting_drafts_auth_item_keys_connection.auth_item_keys_id AS resource_id
           FROM public.setting_drafts_auth_item_keys_connection
          WHERE (setting_drafts_auth_item_keys_connection.active = true)
        UNION ALL
         SELECT setting_drafts_auths_connection.draft_id,
            'auths'::text AS resource_type,
            setting_drafts_auths_connection.auths_id AS resource_id
           FROM public.setting_drafts_auths_connection
          WHERE (setting_drafts_auths_connection.active = true)
        UNION ALL
         SELECT setting_drafts_colors_connection.draft_id,
            'colors'::text AS resource_type,
            setting_drafts_colors_connection.colors_id AS resource_id
           FROM public.setting_drafts_colors_connection
          WHERE (setting_drafts_colors_connection.active = true)
        UNION ALL
         SELECT setting_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            setting_drafts_departments_connection.departments_id AS resource_id
           FROM public.setting_drafts_departments_connection
          WHERE (setting_drafts_departments_connection.active = true)
        UNION ALL
         SELECT setting_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            setting_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.setting_drafts_descriptions_connection
          WHERE (setting_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT setting_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            setting_drafts_flags_connection.flags_id AS resource_id
           FROM public.setting_drafts_flags_connection
          WHERE (setting_drafts_flags_connection.active = true)
        UNION ALL
         SELECT setting_drafts_items_connection.draft_id,
            'items'::text AS resource_type,
            setting_drafts_items_connection.items_id AS resource_id
           FROM public.setting_drafts_items_connection
          WHERE (setting_drafts_items_connection.active = true)
        UNION ALL
         SELECT setting_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            setting_drafts_names_connection.names_id AS resource_id
           FROM public.setting_drafts_names_connection
          WHERE (setting_drafts_names_connection.active = true)
        UNION ALL
         SELECT setting_drafts_profiles_connection.draft_id,
            'profiles'::text AS resource_type,
            setting_drafts_profiles_connection.profiles_id AS resource_id
           FROM public.setting_drafts_profiles_connection
          WHERE (setting_drafts_profiles_connection.active = true)
        UNION ALL
         SELECT setting_drafts_provider_keys_connection.draft_id,
            'provider_keys'::text AS resource_type,
            setting_drafts_provider_keys_connection.provider_keys_id AS resource_id
           FROM public.setting_drafts_provider_keys_connection
          WHERE (setting_drafts_provider_keys_connection.active = true)
        UNION ALL
         SELECT setting_drafts_settings_connection.draft_id,
            'settings'::text AS resource_type,
            setting_drafts_settings_connection.settings_id AS resource_id
           FROM public.setting_drafts_settings_connection
          WHERE (setting_drafts_settings_connection.active = true)
        UNION ALL
         SELECT setting_drafts_thresholds_connection.draft_id,
            'thresholds'::text AS resource_type,
            setting_drafts_thresholds_connection.thresholds_id AS resource_id
           FROM public.setting_drafts_thresholds_connection
          WHERE (setting_drafts_thresholds_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'agents'::text)), ARRAY[]::uuid[]) AS agent_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'auth_item_keys'::text)), ARRAY[]::uuid[]) AS auth_item_key_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'auths'::text)), ARRAY[]::uuid[]) AS auth_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'colors'::text)), ARRAY[]::uuid[]) AS color_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'items'::text)), ARRAY[]::uuid[]) AS item_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'profiles'::text)), ARRAY[]::uuid[]) AS profile_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'provider_keys'::text)), ARRAY[]::uuid[]) AS provider_key_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'settings'::text)), ARRAY[]::uuid[]) AS settings_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'thresholds'::text)), ARRAY[]::uuid[]) AS threshold_ids
   FROM (public.setting_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
