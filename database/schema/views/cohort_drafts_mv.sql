-- Materialized view: cohort_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: cohort_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.cohort_drafts_mv AS
 WITH draft_links AS (
         SELECT cohort_drafts_cohorts_connection.draft_id,
            'cohorts'::text AS resource_type,
            cohort_drafts_cohorts_connection.cohorts_id AS resource_id
           FROM public.cohort_drafts_cohorts_connection
          WHERE (cohort_drafts_cohorts_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            cohort_drafts_departments_connection.departments_id AS resource_id
           FROM public.cohort_drafts_departments_connection
          WHERE (cohort_drafts_departments_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            cohort_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.cohort_drafts_descriptions_connection
          WHERE (cohort_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            cohort_drafts_flags_connection.flags_id AS resource_id
           FROM public.cohort_drafts_flags_connection
          WHERE (cohort_drafts_flags_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            cohort_drafts_names_connection.names_id AS resource_id
           FROM public.cohort_drafts_names_connection
          WHERE (cohort_drafts_names_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_simulation_positions_connection.draft_id,
            'simulation_positions'::text AS resource_type,
            cohort_drafts_simulation_positions_connection.simulation_positions_id AS resource_id
           FROM public.cohort_drafts_simulation_positions_connection
          WHERE (cohort_drafts_simulation_positions_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_simulations_connection.draft_id,
            'simulations'::text AS resource_type,
            cohort_drafts_simulations_connection.simulations_id AS resource_id
           FROM public.cohort_drafts_simulations_connection
          WHERE (cohort_drafts_simulations_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_profile_personas_connection.draft_id,
            'profile_personas'::text AS resource_type,
            cohort_drafts_profile_personas_connection.profile_personas_id AS resource_id
           FROM public.cohort_drafts_profile_personas_connection
          WHERE (cohort_drafts_profile_personas_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_simulation_availability_connection.draft_id,
            'simulation_availability'::text AS resource_type,
            cohort_drafts_simulation_availability_connection.simulation_availability_id AS resource_id
           FROM public.cohort_drafts_simulation_availability_connection
          WHERE (cohort_drafts_simulation_availability_connection.active = true)
        UNION ALL
         SELECT cohort_drafts_profiles_connection.draft_id,
            'profiles'::text AS resource_type,
            cohort_drafts_profiles_connection.profiles_id AS resource_id
           FROM public.cohort_drafts_profiles_connection
          WHERE (cohort_drafts_profiles_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'cohorts'::text)), ARRAY[]::uuid[]) AS cohort_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'simulation_positions'::text)), ARRAY[]::uuid[]) AS simulation_position_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'simulations'::text)), ARRAY[]::uuid[]) AS simulation_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'profile_personas'::text)), ARRAY[]::uuid[]) AS profile_persona_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'simulation_availability'::text)), ARRAY[]::uuid[]) AS simulation_availability_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'profiles'::text)), ARRAY[]::uuid[]) AS profile_ids
   FROM (public.cohort_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
