-- Materialized view: invocation_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: invocation_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.invocation_drafts_mv AS
 WITH draft_links AS (
         SELECT invocation_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            invocation_drafts_departments_connection.departments_id AS resource_id
           FROM public.invocation_drafts_departments_connection
          WHERE (invocation_drafts_departments_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            invocation_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.invocation_drafts_descriptions_connection
          WHERE (invocation_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            invocation_drafts_flags_connection.flags_id AS resource_id
           FROM public.invocation_drafts_flags_connection
          WHERE (invocation_drafts_flags_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_keys_connection.draft_id,
            'keys'::text AS resource_type,
            invocation_drafts_keys_connection.keys_id AS resource_id
           FROM public.invocation_drafts_keys_connection
          WHERE (invocation_drafts_keys_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_model_flags_connection.draft_id,
            'model_flags'::text AS resource_type,
            invocation_drafts_model_flags_connection.model_flags_id AS resource_id
           FROM public.invocation_drafts_model_flags_connection
          WHERE (invocation_drafts_model_flags_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_model_rubrics_connection.draft_id,
            'model_rubrics'::text AS resource_type,
            invocation_drafts_model_rubrics_connection.model_rubrics_id AS resource_id
           FROM public.invocation_drafts_model_rubrics_connection
          WHERE (invocation_drafts_model_rubrics_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_model_positions_connection.draft_id,
            'model_positions'::text AS resource_type,
            invocation_drafts_model_positions_connection.model_positions_id AS resource_id
           FROM public.invocation_drafts_model_positions_connection
          WHERE (invocation_drafts_model_positions_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            invocation_drafts_names_connection.names_id AS resource_id
           FROM public.invocation_drafts_names_connection
          WHERE (invocation_drafts_names_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_reasoning_levels_connection.draft_id,
            'reasoning_levels'::text AS resource_type,
            invocation_drafts_reasoning_levels_connection.reasoning_levels_id AS resource_id
           FROM public.invocation_drafts_reasoning_levels_connection
          WHERE (invocation_drafts_reasoning_levels_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_temperature_levels_connection.draft_id,
            'temperature_levels'::text AS resource_type,
            invocation_drafts_temperature_levels_connection.temperature_levels_id AS resource_id
           FROM public.invocation_drafts_temperature_levels_connection
          WHERE (invocation_drafts_temperature_levels_connection.active = true)
        UNION ALL
         SELECT invocation_drafts_voices_connection.draft_id,
            'voices'::text AS resource_type,
            invocation_drafts_voices_connection.voices_id AS resource_id
           FROM public.invocation_drafts_voices_connection
          WHERE (invocation_drafts_voices_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'keys'::text)), ARRAY[]::uuid[]) AS key_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'model_flags'::text)), ARRAY[]::uuid[]) AS model_flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'model_rubrics'::text)), ARRAY[]::uuid[]) AS model_rubric_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'model_positions'::text)), ARRAY[]::uuid[]) AS model_position_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'reasoning_levels'::text)), ARRAY[]::uuid[]) AS reasoning_level_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'temperature_levels'::text)), ARRAY[]::uuid[]) AS temperature_level_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'voices'::text)), ARRAY[]::uuid[]) AS voice_ids
   FROM (public.invocation_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
