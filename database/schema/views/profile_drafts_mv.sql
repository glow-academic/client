-- Materialized view: profile_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: profile_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.profile_drafts_mv AS
 WITH draft_links AS (
         SELECT profile_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            profile_drafts_departments_connection.departments_id AS resource_id
           FROM public.profile_drafts_departments_connection
          WHERE (profile_drafts_departments_connection.active = true)
        UNION ALL
         SELECT profile_drafts_emails_connection.draft_id,
            'emails'::text AS resource_type,
            profile_drafts_emails_connection.emails_id AS resource_id
           FROM public.profile_drafts_emails_connection
          WHERE (profile_drafts_emails_connection.active = true)
        UNION ALL
         SELECT profile_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            profile_drafts_flags_connection.flags_id AS resource_id
           FROM public.profile_drafts_flags_connection
          WHERE (profile_drafts_flags_connection.active = true)
        UNION ALL
         SELECT profile_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            profile_drafts_names_connection.names_id AS resource_id
           FROM public.profile_drafts_names_connection
          WHERE (profile_drafts_names_connection.active = true)
        UNION ALL
         SELECT profile_drafts_profiles_connection.draft_id,
            'profiles'::text AS resource_type,
            profile_drafts_profiles_connection.profiles_id AS resource_id
           FROM public.profile_drafts_profiles_connection
          WHERE (profile_drafts_profiles_connection.active = true)
        UNION ALL
         SELECT profile_drafts_request_limits_connection.draft_id,
            'request_limits'::text AS resource_type,
            profile_drafts_request_limits_connection.request_limits_id AS resource_id
           FROM public.profile_drafts_request_limits_connection
          WHERE (profile_drafts_request_limits_connection.active = true)
        UNION ALL
         SELECT profile_drafts_roles_connection.draft_id,
            'roles'::text AS resource_type,
            profile_drafts_roles_connection.roles_id AS resource_id
           FROM public.profile_drafts_roles_connection
          WHERE (profile_drafts_roles_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'emails'::text)), ARRAY[]::uuid[]) AS email_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'profiles'::text)), ARRAY[]::uuid[]) AS profile_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'request_limits'::text)), ARRAY[]::uuid[]) AS request_limit_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'roles'::text)), ARRAY[]::uuid[]) AS role_ids
   FROM (public.profile_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
