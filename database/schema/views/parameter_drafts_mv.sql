-- Materialized view: parameter_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: parameter_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.parameter_drafts_mv AS
 WITH draft_links AS (
         SELECT parameter_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            parameter_drafts_departments_connection.departments_id AS resource_id
           FROM public.parameter_drafts_departments_connection
          WHERE (parameter_drafts_departments_connection.active = true)
        UNION ALL
         SELECT parameter_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            parameter_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.parameter_drafts_descriptions_connection
          WHERE (parameter_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT parameter_drafts_fields_connection.draft_id,
            'fields'::text AS resource_type,
            parameter_drafts_fields_connection.fields_id AS resource_id
           FROM public.parameter_drafts_fields_connection
          WHERE (parameter_drafts_fields_connection.active = true)
        UNION ALL
         SELECT parameter_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            parameter_drafts_flags_connection.flags_id AS resource_id
           FROM public.parameter_drafts_flags_connection
          WHERE (parameter_drafts_flags_connection.active = true)
        UNION ALL
         SELECT parameter_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            parameter_drafts_names_connection.names_id AS resource_id
           FROM public.parameter_drafts_names_connection
          WHERE (parameter_drafts_names_connection.active = true)
        UNION ALL
         SELECT parameter_drafts_parameters_connection.draft_id,
            'parameters'::text AS resource_type,
            parameter_drafts_parameters_connection.parameters_id AS resource_id
           FROM public.parameter_drafts_parameters_connection
          WHERE (parameter_drafts_parameters_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'fields'::text)), ARRAY[]::uuid[]) AS field_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'parameters'::text)), ARRAY[]::uuid[]) AS parameter_ids
   FROM (public.parameter_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
