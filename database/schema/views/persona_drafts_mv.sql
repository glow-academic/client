-- Materialized view: persona_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: persona_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.persona_drafts_mv AS
 WITH draft_links AS (
         SELECT persona_drafts_colors_connection.draft_id,
            'colors'::text AS resource_type,
            persona_drafts_colors_connection.colors_id AS resource_id
           FROM public.persona_drafts_colors_connection
          WHERE (persona_drafts_colors_connection.active = true)
        UNION ALL
         SELECT persona_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            persona_drafts_departments_connection.departments_id AS resource_id
           FROM public.persona_drafts_departments_connection
          WHERE (persona_drafts_departments_connection.active = true)
        UNION ALL
         SELECT persona_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            persona_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.persona_drafts_descriptions_connection
          WHERE (persona_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT persona_drafts_examples_connection.draft_id,
            'examples'::text AS resource_type,
            persona_drafts_examples_connection.examples_id AS resource_id
           FROM public.persona_drafts_examples_connection
          WHERE (persona_drafts_examples_connection.active = true)
        UNION ALL
         SELECT persona_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            persona_drafts_flags_connection.flags_id AS resource_id
           FROM public.persona_drafts_flags_connection
          WHERE (persona_drafts_flags_connection.active = true)
        UNION ALL
         SELECT persona_drafts_icons_connection.draft_id,
            'icons'::text AS resource_type,
            persona_drafts_icons_connection.icons_id AS resource_id
           FROM public.persona_drafts_icons_connection
          WHERE (persona_drafts_icons_connection.active = true)
        UNION ALL
         SELECT persona_drafts_instructions_connection.draft_id,
            'instructions'::text AS resource_type,
            persona_drafts_instructions_connection.instructions_id AS resource_id
           FROM public.persona_drafts_instructions_connection
          WHERE (persona_drafts_instructions_connection.active = true)
        UNION ALL
         SELECT persona_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            persona_drafts_names_connection.names_id AS resource_id
           FROM public.persona_drafts_names_connection
          WHERE (persona_drafts_names_connection.active = true)
        UNION ALL
         SELECT persona_drafts_parameter_fields_connection.draft_id,
            'parameter_fields'::text AS resource_type,
            persona_drafts_parameter_fields_connection.parameter_fields_id AS resource_id
           FROM public.persona_drafts_parameter_fields_connection
          WHERE (persona_drafts_parameter_fields_connection.active = true)
        UNION ALL
         SELECT persona_drafts_personas_connection.draft_id,
            'personas'::text AS resource_type,
            persona_drafts_personas_connection.personas_id AS resource_id
           FROM public.persona_drafts_personas_connection
          WHERE (persona_drafts_personas_connection.active = true)
        UNION ALL
         SELECT persona_drafts_voices_connection.draft_id,
            'voices'::text AS resource_type,
            persona_drafts_voices_connection.voices_id AS resource_id
           FROM public.persona_drafts_voices_connection
          WHERE (persona_drafts_voices_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'colors'::text)), ARRAY[]::uuid[]) AS color_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'examples'::text)), ARRAY[]::uuid[]) AS example_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'icons'::text)), ARRAY[]::uuid[]) AS icon_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'instructions'::text)), ARRAY[]::uuid[]) AS instruction_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'parameter_fields'::text)), ARRAY[]::uuid[]) AS parameter_field_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'parameters'::text)), ARRAY[]::uuid[]) AS parameter_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'personas'::text)), ARRAY[]::uuid[]) AS persona_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'voices'::text)), ARRAY[]::uuid[]) AS voice_ids
   FROM (public.persona_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  WHERE (d.active = true)
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
