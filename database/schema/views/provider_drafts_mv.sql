-- Materialized view: provider_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: provider_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.provider_drafts_mv AS
 WITH draft_links AS (
         SELECT provider_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            provider_drafts_departments_connection.departments_id AS resource_id
           FROM public.provider_drafts_departments_connection
          WHERE (provider_drafts_departments_connection.active = true)
        UNION ALL
         SELECT provider_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            provider_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.provider_drafts_descriptions_connection
          WHERE (provider_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT provider_drafts_endpoints_connection.draft_id,
            'endpoints'::text AS resource_type,
            provider_drafts_endpoints_connection.endpoints_id AS resource_id
           FROM public.provider_drafts_endpoints_connection
          WHERE (provider_drafts_endpoints_connection.active = true)
        UNION ALL
         SELECT provider_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            provider_drafts_flags_connection.flags_id AS resource_id
           FROM public.provider_drafts_flags_connection
          WHERE (provider_drafts_flags_connection.active = true)
        UNION ALL
         SELECT provider_drafts_keys_connection.draft_id,
            'keys'::text AS resource_type,
            provider_drafts_keys_connection.keys_id AS resource_id
           FROM public.provider_drafts_keys_connection
          WHERE (provider_drafts_keys_connection.active = true)
        UNION ALL
         SELECT provider_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            provider_drafts_names_connection.names_id AS resource_id
           FROM public.provider_drafts_names_connection
          WHERE (provider_drafts_names_connection.active = true)
        UNION ALL
         SELECT provider_drafts_providers_connection.draft_id,
            'providers'::text AS resource_type,
            provider_drafts_providers_connection.providers_id AS resource_id
           FROM public.provider_drafts_providers_connection
          WHERE (provider_drafts_providers_connection.active = true)
        UNION ALL
         SELECT provider_drafts_values_connection.draft_id,
            'values'::text AS resource_type,
            provider_drafts_values_connection.values_id AS resource_id
           FROM public.provider_drafts_values_connection
          WHERE (provider_drafts_values_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'endpoints'::text)), ARRAY[]::uuid[]) AS endpoint_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'keys'::text)), ARRAY[]::uuid[]) AS key_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'providers'::text)), ARRAY[]::uuid[]) AS provider_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'values'::text)), ARRAY[]::uuid[]) AS value_ids
   FROM (public.provider_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
