-- Materialized view: logins_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: logins_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.logins_mv AS
 SELECT l.id AS login_id,
    plc.profiles_id AS profile_id,
    l.session_id,
    l.last_login,
    l.created_at AS login_created_at,
    COALESCE(l.active, false) AS active,
    l.generated,
    l.mcp
   FROM (public.logins_entry l
     LEFT JOIN public.profiles_logins_connection plc ON (((plc.login_id = l.id) AND (plc.active = true))))
  WITH NO DATA;


--
