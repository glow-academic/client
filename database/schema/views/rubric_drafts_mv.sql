-- Materialized view: rubric_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: rubric_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.rubric_drafts_mv AS
 WITH draft_links AS (
         SELECT rubric_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            rubric_drafts_departments_connection.departments_id AS resource_id
           FROM public.rubric_drafts_departments_connection
          WHERE (rubric_drafts_departments_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            rubric_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.rubric_drafts_descriptions_connection
          WHERE (rubric_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            rubric_drafts_flags_connection.flags_id AS resource_id
           FROM public.rubric_drafts_flags_connection
          WHERE (rubric_drafts_flags_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            rubric_drafts_names_connection.names_id AS resource_id
           FROM public.rubric_drafts_names_connection
          WHERE (rubric_drafts_names_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_points_connection.draft_id,
            'points'::text AS resource_type,
            rubric_drafts_points_connection.points_id AS resource_id
           FROM public.rubric_drafts_points_connection
          WHERE (rubric_drafts_points_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_rubrics_connection.draft_id,
            'rubrics'::text AS resource_type,
            rubric_drafts_rubrics_connection.rubrics_id AS resource_id
           FROM public.rubric_drafts_rubrics_connection
          WHERE (rubric_drafts_rubrics_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_standard_groups_connection.draft_id,
            'standard_groups'::text AS resource_type,
            rubric_drafts_standard_groups_connection.standard_groups_id AS resource_id
           FROM public.rubric_drafts_standard_groups_connection
          WHERE (rubric_drafts_standard_groups_connection.active = true)
        UNION ALL
         SELECT rubric_drafts_standards_connection.draft_id,
            'standards'::text AS resource_type,
            rubric_drafts_standards_connection.standards_id AS resource_id
           FROM public.rubric_drafts_standards_connection
          WHERE (rubric_drafts_standards_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'points'::text)), ARRAY[]::uuid[]) AS point_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'rubrics'::text)), ARRAY[]::uuid[]) AS rubric_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'standard_groups'::text)), ARRAY[]::uuid[]) AS standard_group_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'standards'::text)), ARRAY[]::uuid[]) AS standard_ids
   FROM (public.rubric_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
