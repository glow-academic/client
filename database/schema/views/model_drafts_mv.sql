-- Materialized view: model_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: model_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.model_drafts_mv AS
 WITH draft_links AS (
         SELECT model_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            model_drafts_departments_connection.departments_id AS resource_id
           FROM public.model_drafts_departments_connection
          WHERE (model_drafts_departments_connection.active = true)
        UNION ALL
         SELECT model_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            model_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.model_drafts_descriptions_connection
          WHERE (model_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT model_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            model_drafts_flags_connection.flags_id AS resource_id
           FROM public.model_drafts_flags_connection
          WHERE (model_drafts_flags_connection.active = true)
        UNION ALL
         SELECT model_drafts_modalities_connection.draft_id,
            'modalities'::text AS resource_type,
            model_drafts_modalities_connection.modalities_id AS resource_id
           FROM public.model_drafts_modalities_connection
          WHERE (model_drafts_modalities_connection.active = true)
        UNION ALL
         SELECT model_drafts_models_connection.draft_id,
            'models'::text AS resource_type,
            model_drafts_models_connection.models_id AS resource_id
           FROM public.model_drafts_models_connection
          WHERE (model_drafts_models_connection.active = true)
        UNION ALL
         SELECT model_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            model_drafts_names_connection.names_id AS resource_id
           FROM public.model_drafts_names_connection
          WHERE (model_drafts_names_connection.active = true)
        UNION ALL
         SELECT model_drafts_pricing_connection.draft_id,
            'pricing'::text AS resource_type,
            model_drafts_pricing_connection.pricing_id AS resource_id
           FROM public.model_drafts_pricing_connection
          WHERE (model_drafts_pricing_connection.active = true)
        UNION ALL
         SELECT model_drafts_providers_connection.draft_id,
            'providers'::text AS resource_type,
            model_drafts_providers_connection.providers_id AS resource_id
           FROM public.model_drafts_providers_connection
          WHERE (model_drafts_providers_connection.active = true)
        UNION ALL
         SELECT model_drafts_qualities_connection.draft_id,
            'qualities'::text AS resource_type,
            model_drafts_qualities_connection.qualities_id AS resource_id
           FROM public.model_drafts_qualities_connection
          WHERE (model_drafts_qualities_connection.active = true)
        UNION ALL
         SELECT model_drafts_reasoning_levels_connection.draft_id,
            'reasoning_levels'::text AS resource_type,
            model_drafts_reasoning_levels_connection.reasoning_levels_id AS resource_id
           FROM public.model_drafts_reasoning_levels_connection
          WHERE (model_drafts_reasoning_levels_connection.active = true)
        UNION ALL
         SELECT model_drafts_temperature_levels_connection.draft_id,
            'temperature_levels'::text AS resource_type,
            model_drafts_temperature_levels_connection.temperature_levels_id AS resource_id
           FROM public.model_drafts_temperature_levels_connection
          WHERE (model_drafts_temperature_levels_connection.active = true)
        UNION ALL
         SELECT model_drafts_values_connection.draft_id,
            'values'::text AS resource_type,
            model_drafts_values_connection.values_id AS resource_id
           FROM public.model_drafts_values_connection
          WHERE (model_drafts_values_connection.active = true)
        UNION ALL
         SELECT model_drafts_voices_connection.draft_id,
            'voices'::text AS resource_type,
            model_drafts_voices_connection.voices_id AS resource_id
           FROM public.model_drafts_voices_connection
          WHERE (model_drafts_voices_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'modalities'::text)), ARRAY[]::uuid[]) AS modality_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'models'::text)), ARRAY[]::uuid[]) AS model_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'pricing'::text)), ARRAY[]::uuid[]) AS pricing_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'providers'::text)), ARRAY[]::uuid[]) AS provider_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'qualities'::text)), ARRAY[]::uuid[]) AS quality_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'reasoning_levels'::text)), ARRAY[]::uuid[]) AS reasoning_level_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'temperature_levels'::text)), ARRAY[]::uuid[]) AS temperature_level_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'values'::text)), ARRAY[]::uuid[]) AS value_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'voices'::text)), ARRAY[]::uuid[]) AS voice_ids
   FROM (public.model_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
