-- Materialized view: auth_drafts_mv
-- Auto-generated by split_schema.py — do not edit manually
--

-- Name: auth_drafts_mv; Type: MATERIALIZED VIEW; Schema: public; Owner: -
--

CREATE MATERIALIZED VIEW public.auth_drafts_mv AS
 WITH draft_links AS (
         SELECT auth_drafts_auths_connection.draft_id,
            'auths'::text AS resource_type,
            auth_drafts_auths_connection.auths_id AS resource_id
           FROM public.auth_drafts_auths_connection
          WHERE (auth_drafts_auths_connection.active = true)
        UNION ALL
         SELECT auth_drafts_departments_connection.draft_id,
            'departments'::text AS resource_type,
            auth_drafts_departments_connection.departments_id AS resource_id
           FROM public.auth_drafts_departments_connection
          WHERE (auth_drafts_departments_connection.active = true)
        UNION ALL
         SELECT auth_drafts_descriptions_connection.draft_id,
            'descriptions'::text AS resource_type,
            auth_drafts_descriptions_connection.descriptions_id AS resource_id
           FROM public.auth_drafts_descriptions_connection
          WHERE (auth_drafts_descriptions_connection.active = true)
        UNION ALL
         SELECT auth_drafts_flags_connection.draft_id,
            'flags'::text AS resource_type,
            auth_drafts_flags_connection.flags_id AS resource_id
           FROM public.auth_drafts_flags_connection
          WHERE (auth_drafts_flags_connection.active = true)
        UNION ALL
         SELECT auth_drafts_items_connection.draft_id,
            'items'::text AS resource_type,
            auth_drafts_items_connection.items_id AS resource_id
           FROM public.auth_drafts_items_connection
          WHERE (auth_drafts_items_connection.active = true)
        UNION ALL
         SELECT auth_drafts_names_connection.draft_id,
            'names'::text AS resource_type,
            auth_drafts_names_connection.names_id AS resource_id
           FROM public.auth_drafts_names_connection
          WHERE (auth_drafts_names_connection.active = true)
        UNION ALL
         SELECT auth_drafts_protocols_connection.draft_id,
            'protocols'::text AS resource_type,
            auth_drafts_protocols_connection.protocols_id AS resource_id
           FROM public.auth_drafts_protocols_connection
          WHERE (auth_drafts_protocols_connection.active = true)
        UNION ALL
         SELECT auth_drafts_slugs_connection.draft_id,
            'slugs'::text AS resource_type,
            auth_drafts_slugs_connection.slugs_id AS resource_id
           FROM public.auth_drafts_slugs_connection
          WHERE (auth_drafts_slugs_connection.active = true)
        )
 SELECT d.id AS draft_id,
    d.created_at,
    d.updated_at,
    d.version,
    d.generated,
    d.mcp,
    d.active,
    d.group_id,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'auths'::text)), ARRAY[]::uuid[]) AS auth_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'departments'::text)), ARRAY[]::uuid[]) AS department_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'descriptions'::text)), ARRAY[]::uuid[]) AS description_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'flags'::text)), ARRAY[]::uuid[]) AS flag_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'items'::text)), ARRAY[]::uuid[]) AS item_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'names'::text)), ARRAY[]::uuid[]) AS name_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'protocols'::text)), ARRAY[]::uuid[]) AS protocol_ids,
    COALESCE(array_agg(DISTINCT l.resource_id) FILTER (WHERE (l.resource_type = 'slugs'::text)), ARRAY[]::uuid[]) AS slug_ids
   FROM (public.auth_drafts_entry d
     LEFT JOIN draft_links l ON ((l.draft_id = d.id)))
  GROUP BY d.id, d.created_at, d.updated_at, d.version, d.generated, d.mcp, d.active, d.group_id
  WITH NO DATA;


--
