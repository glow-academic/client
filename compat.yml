# Compatibility declaration for glow-client releases.
# Uploaded as compat.json release asset by the tag workflow.
# The LearnLoop deploy API reads this to filter compatible versions.

component: glow-client
min_api: "1.0.0"
max_api_major: 2
