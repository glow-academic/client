/**
 * NewEval.tsx
 * Used to display the new eval page.
 * @AshokSaravanan222 & @siladiea
 * 06/07/2025
 */

import Eval from "@/components/common/eval/Eval";

export default function NewEval() {
    return <Eval mode="create" />;
}