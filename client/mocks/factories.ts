// This file is auto-generated by the test harness generator. Do not edit manually.
import { faker } from '@faker-js/faker';
import type { Agent, AppFeedback, AppLog, AssistantChat, AssistantMessage, AssistantToolCall, CohortProfile, CohortSimulation, Cohort, DebugInfo, DepartmentAgent, Department, Document, ModelRun, Model, ParameterItem, Parameter, Persona, ProfileDepartment, Profile, Provider, Rubric, ScenarioDocument, ScenarioObjective, ScenarioParameterItem, ScenarioTree, Scenario, SimulationAttempt, SimulationChatFeedback, SimulationChatGrade, SimulationChat, SimulationHint, SimulationMessage, SimulationScenario, SimulationTagDocument, SimulationTagParameterItem, SimulationTag, Simulation, StandardGroup, Standard } from '@/types';
import { assistantMessageType, assistantToolType, documentType, feedbackType, profileRole, reasoningEffort, simulationMessageType } from '@/utils/drizzle/schema';


export function createMockAgent(overrides: Partial<Agent> = {}): Agent {
  const defaults: Agent = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    systemPrompt: faker.lorem.word(),
    temperature: faker.number.int({ min: 1, max: 1000 }),
    modelId: faker.string.uuid(),
    reasoning: faker.helpers.arrayElement(reasoningEffort.enumValues),
  };

  return { ...defaults, ...overrides } as Agent;
}

export function createMockAppFeedback(overrides: Partial<AppFeedback> = {}): AppFeedback {
  const defaults: AppFeedback = {
    id: faker.number.int(),
    createdAt: faker.date.past().toISOString(),
    profileId: faker.string.uuid(),
    type: faker.helpers.arrayElement(feedbackType.enumValues),
    message: faker.lorem.paragraph(),
  };

  return { ...defaults, ...overrides } as AppFeedback;
}

export function createMockAppLog(overrides: Partial<AppLog> = {}): AppLog {
  const defaults: AppLog = {
    id: faker.number.int(),
    event: faker.lorem.word(),
    level: faker.lorem.word(),
    message: faker.lorem.paragraph(),
    correlationId: faker.lorem.word(),
    actor: {},
    subject: {},
    metrics: {},
    context: {},
    error: {},
    createdAt: faker.date.past().toISOString(),
  };

  return { ...defaults, ...overrides } as AppLog;
}

export function createMockAssistantChat(overrides: Partial<AssistantChat> = {}): AssistantChat {
  const defaults: AssistantChat = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    title: faker.lorem.words(3),
    profileId: faker.string.uuid(),
    traceId: faker.lorem.word(),
  };

  return { ...defaults, ...overrides } as AssistantChat;
}

export function createMockAssistantMessage(overrides: Partial<AssistantMessage> = {}): AssistantMessage {
  const defaults: AssistantMessage = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    completedAt: faker.date.past().toISOString(),
    chatId: faker.string.uuid(),
    role: faker.helpers.arrayElement(assistantMessageType.enumValues),
    content: faker.lorem.paragraph(),
    completed: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as AssistantMessage;
}

export function createMockAssistantToolCall(overrides: Partial<AssistantToolCall> = {}): AssistantToolCall {
  const defaults: AssistantToolCall = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    completedAt: faker.date.past().toISOString(),
    chatId: faker.string.uuid(),
    toolName: faker.lorem.words(3),
    toolType: faker.helpers.arrayElement(assistantToolType.enumValues),
    toolArguments: {},
    toolResult: {},
    completed: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as AssistantToolCall;
}

export function createMockCohortProfile(overrides: Partial<CohortProfile> = {}): CohortProfile {
  const defaults: CohortProfile = {
    cohortId: faker.string.uuid(),
    profileId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as CohortProfile;
}

export function createMockCohortSimulation(overrides: Partial<CohortSimulation> = {}): CohortSimulation {
  const defaults: CohortSimulation = {
    cohortId: faker.string.uuid(),
    simulationId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as CohortSimulation;
}

export function createMockCohort(overrides: Partial<Cohort> = {}): Cohort {
  const defaults: Cohort = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    title: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    active: faker.datatype.boolean(),
    defaultCohort: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Cohort;
}

export function createMockDebugInfo(overrides: Partial<DebugInfo> = {}): DebugInfo {
  const defaults: DebugInfo = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    modelRunId: faker.string.uuid(),
    content: faker.lorem.paragraph(),
  };

  return { ...defaults, ...overrides } as DebugInfo;
}

export function createMockDepartmentAgent(overrides: Partial<DepartmentAgent> = {}): DepartmentAgent {
  const defaults: DepartmentAgent = {
    departmentId: faker.string.uuid(),
    role: faker.string.uuid(),
    agentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as DepartmentAgent;
}

export function createMockDepartment(overrides: Partial<Department> = {}): Department {
  const defaults: Department = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    title: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    active: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as Department;
}

export function createMockDocument(overrides: Partial<Document> = {}): Document {
  const defaults: Document = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    filePath: faker.string.alphanumeric(32),
    mimeType: faker.lorem.word(),
    type: faker.helpers.arrayElement(documentType.enumValues),
    classified: faker.datatype.boolean(),
    fileId: faker.lorem.word(),
    active: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Document;
}

export function createMockModelRun(overrides: Partial<ModelRun> = {}): ModelRun {
  const defaults: ModelRun = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    modelId: faker.string.uuid(),
    inputTokens: faker.number.int({ min: 1, max: 1000 }),
    outputTokens: faker.number.int({ min: 1, max: 1000 }),
    personaId: faker.string.uuid(),
    agentId: faker.string.uuid(),
    profileId: faker.string.uuid(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as ModelRun;
}

export function createMockModel(overrides: Partial<Model> = {}): Model {
  const defaults: Model = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    providerId: faker.string.uuid(),
    active: faker.datatype.boolean(),
    inputPpm: faker.number.int({ min: 1, max: 1000 }),
    outputPpm: faker.number.int({ min: 1, max: 1000 }),
    customModel: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as Model;
}

export function createMockParameterItem(overrides: Partial<ParameterItem> = {}): ParameterItem {
  const defaults: ParameterItem = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    value: faker.lorem.word(),
    parameterId: faker.string.uuid(),
    defaultItem: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as ParameterItem;
}

export function createMockParameter(overrides: Partial<Parameter> = {}): Parameter {
  const defaults: Parameter = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    numerical: faker.datatype.boolean(),
    active: faker.datatype.boolean(),
    defaultParameter: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Parameter;
}

export function createMockPersona(overrides: Partial<Persona> = {}): Persona {
  const defaults: Persona = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    systemPrompt: faker.lorem.word(),
    temperature: faker.number.int({ min: 1, max: 1000 }),
    defaultPersona: faker.datatype.boolean(),
    color: faker.lorem.word(),
    icon: faker.lorem.word(),
    modelId: faker.string.uuid(),
    reasoning: faker.helpers.arrayElement(reasoningEffort.enumValues),
    active: faker.datatype.boolean(),
    guardrailActive: faker.datatype.boolean(),
    imageInputActive: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Persona;
}

export function createMockProfileDepartment(overrides: Partial<ProfileDepartment> = {}): ProfileDepartment {
  const defaults: ProfileDepartment = {
    profileId: faker.string.uuid(),
    departmentId: faker.string.uuid(),
    isPrimary: faker.datatype.boolean(),
    createdAt: faker.date.past().toISOString(),
  };

  return { ...defaults, ...overrides } as ProfileDepartment;
}

export function createMockProfile(overrides: Partial<Profile> = {}): Profile {
  const defaults: Profile = {
    id: faker.string.uuid(),
    updatedAt: faker.date.past().toISOString(),
    userId: faker.number.int({ min: 1, max: 1000 }),
    lastLogin: faker.date.past().toISOString(),
    firstName: faker.person.firstName(),
    lastName: faker.person.lastName(),
    alias: faker.lorem.word(),
    viewedIntro: faker.datatype.boolean(),
    viewedChat: faker.datatype.boolean(),
    createdAt: faker.date.past().toISOString(),
    role: faker.helpers.arrayElement(profileRole.enumValues),
    defaultProfile: faker.datatype.boolean(),
    active: faker.datatype.boolean(),
    lastActive: faker.date.past().toISOString(),
    reqPerDay: faker.number.int({ min: 1, max: 1000 }),
  };

  return { ...defaults, ...overrides } as Profile;
}

export function createMockProvider(overrides: Partial<Provider> = {}): Provider {
  const defaults: Provider = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    apiKey: faker.lorem.word(),
    baseUrl: faker.internet.url(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Provider;
}

export function createMockRubric(overrides: Partial<Rubric> = {}): Rubric {
  const defaults: Rubric = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    points: faker.number.int({ min: 1, max: 1000 }),
    passPoints: faker.number.int({ min: 1, max: 1000 }),
    defaultRubric: faker.datatype.boolean(),
    active: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Rubric;
}

export function createMockScenarioDocument(overrides: Partial<ScenarioDocument> = {}): ScenarioDocument {
  const defaults: ScenarioDocument = {
    scenarioId: faker.string.uuid(),
    documentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as ScenarioDocument;
}

export function createMockScenarioObjective(overrides: Partial<ScenarioObjective> = {}): ScenarioObjective {
  const defaults: ScenarioObjective = {
    scenarioId: faker.string.uuid(),
    idx: faker.number.int(),
    objective: faker.lorem.word(),
  };

  return { ...defaults, ...overrides } as ScenarioObjective;
}

export function createMockScenarioParameterItem(overrides: Partial<ScenarioParameterItem> = {}): ScenarioParameterItem {
  const defaults: ScenarioParameterItem = {
    scenarioId: faker.string.uuid(),
    parameterItemId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as ScenarioParameterItem;
}

export function createMockScenarioTree(overrides: Partial<ScenarioTree> = {}): ScenarioTree {
  const defaults: ScenarioTree = {
    parentId: faker.string.uuid(),
    childId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as ScenarioTree;
}

export function createMockScenario(overrides: Partial<Scenario> = {}): Scenario {
  const defaults: Scenario = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    problemStatement: faker.lorem.word(),
    personaId: faker.string.uuid(),
    defaultScenario: faker.datatype.boolean(),
    generated: faker.datatype.boolean(),
    active: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Scenario;
}

export function createMockSimulationAttempt(overrides: Partial<SimulationAttempt> = {}): SimulationAttempt {
  const defaults: SimulationAttempt = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    profileId: faker.string.uuid(),
    simulationId: faker.string.uuid(),
    infiniteMode: faker.datatype.boolean(),
    infiniteModeTimeLimit: faker.number.int({ min: 1, max: 1000 }),
    archived: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as SimulationAttempt;
}

export function createMockSimulationChatFeedback(overrides: Partial<SimulationChatFeedback> = {}): SimulationChatFeedback {
  const defaults: SimulationChatFeedback = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    standardId: faker.string.uuid(),
    simulationChatGradeId: faker.string.uuid(),
    total: faker.number.int({ min: 1, max: 1000 }),
    feedback: faker.lorem.word(),
  };

  return { ...defaults, ...overrides } as SimulationChatFeedback;
}

export function createMockSimulationChatGrade(overrides: Partial<SimulationChatGrade> = {}): SimulationChatGrade {
  const defaults: SimulationChatGrade = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    description: faker.lorem.paragraph(),
    passed: faker.datatype.boolean(),
    score: faker.number.int({ min: 1, max: 1000 }),
    timeTaken: faker.number.int({ min: 1, max: 1000 }),
    rubricId: faker.string.uuid(),
    simulationChatId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as SimulationChatGrade;
}

export function createMockSimulationChat(overrides: Partial<SimulationChat> = {}): SimulationChat {
  const defaults: SimulationChat = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    completedAt: faker.date.past().toISOString(),
    title: faker.lorem.words(3),
    scenarioId: faker.string.uuid(),
    attemptId: faker.string.uuid(),
    completed: faker.datatype.boolean(),
    traceId: faker.lorem.word(),
  };

  return { ...defaults, ...overrides } as SimulationChat;
}

export function createMockSimulationHint(overrides: Partial<SimulationHint> = {}): SimulationHint {
  const defaults: SimulationHint = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    hint: faker.lorem.word(),
    simulationMessageId: faker.lorem.paragraph(),
  };

  return { ...defaults, ...overrides } as SimulationHint;
}

export function createMockSimulationMessage(overrides: Partial<SimulationMessage> = {}): SimulationMessage {
  const defaults: SimulationMessage = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    chatId: faker.string.uuid(),
    content: faker.lorem.paragraph(),
    type: faker.helpers.arrayElement(simulationMessageType.enumValues),
    completed: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as SimulationMessage;
}

export function createMockSimulationScenario(overrides: Partial<SimulationScenario> = {}): SimulationScenario {
  const defaults: SimulationScenario = {
    simulationId: faker.string.uuid(),
    scenarioId: faker.string.uuid(),
    position: faker.number.int({ min: 1, max: 1000 }),
  };

  return { ...defaults, ...overrides } as SimulationScenario;
}

export function createMockSimulationTagDocument(overrides: Partial<SimulationTagDocument> = {}): SimulationTagDocument {
  const defaults: SimulationTagDocument = {
    simulationId: faker.string.uuid(),
    tagIdx: faker.number.int(),
    documentId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as SimulationTagDocument;
}

export function createMockSimulationTagParameterItem(overrides: Partial<SimulationTagParameterItem> = {}): SimulationTagParameterItem {
  const defaults: SimulationTagParameterItem = {
    simulationId: faker.string.uuid(),
    tagIdx: faker.number.int(),
    parameterItemId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as SimulationTagParameterItem;
}

export function createMockSimulationTag(overrides: Partial<SimulationTag> = {}): SimulationTag {
  const defaults: SimulationTag = {
    simulationId: faker.string.uuid(),
    idx: faker.number.int(),
    tag: faker.lorem.word(),
  };

  return { ...defaults, ...overrides } as SimulationTag;
}

export function createMockSimulation(overrides: Partial<Simulation> = {}): Simulation {
  const defaults: Simulation = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    updatedAt: faker.date.past().toISOString(),
    title: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    timeLimit: faker.number.int({ min: 1, max: 1000 }),
    active: faker.datatype.boolean(),
    rubricId: faker.string.uuid(),
    defaultSimulation: faker.datatype.boolean(),
    practiceSimulation: faker.datatype.boolean(),
    departmentId: faker.string.uuid(),
    outputGuardrailActive: faker.datatype.boolean(),
    inputGuardrailActive: faker.datatype.boolean(),
    imageInputActive: faker.datatype.boolean(),
    hintsEnabled: faker.datatype.boolean(),
  };

  return { ...defaults, ...overrides } as Simulation;
}

export function createMockStandardGroup(overrides: Partial<StandardGroup> = {}): StandardGroup {
  const defaults: StandardGroup = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    shortName: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    points: faker.number.int({ min: 1, max: 1000 }),
    passPoints: faker.number.int({ min: 1, max: 1000 }),
    rubricId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as StandardGroup;
}

export function createMockStandard(overrides: Partial<Standard> = {}): Standard {
  const defaults: Standard = {
    id: faker.string.uuid(),
    createdAt: faker.date.past().toISOString(),
    name: faker.lorem.words(3),
    description: faker.lorem.paragraph(),
    points: faker.number.int({ min: 1, max: 1000 }),
    standardGroupId: faker.string.uuid(),
  };

  return { ...defaults, ...overrides } as Standard;
}
