"""Suite artifact endpoints - operational view of benchmark.

Suite is to benchmark what training is to home: the operational/execution layer.
Full implementation (get, draft, refresh, docs) is deferred.
"""

from fastapi import APIRouter

router = APIRouter()
