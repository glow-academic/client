"""Handler for generate_audio_complete WebSocket event - placeholder (audio doesn't dispatch completion)."""

# Audio generation does NOT dispatch completion events
# This is a placeholder stub

from fastapi import APIRouter

server_router = APIRouter()

# TODO: Audio doesn't dispatch completion events - frontend handles completion
# This file is kept for consistency but is not used
