"""Handler for generate_audio_progress WebSocket event - placeholder (audio doesn't dispatch progress)."""

# Audio generation does NOT dispatch progress events
# This is a placeholder stub

from fastapi import APIRouter

server_router = APIRouter()

# TODO: Audio doesn't dispatch progress events - frontend handles progress/completion
# This file is kept for consistency but is not used
