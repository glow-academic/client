[tool:pytest]
# Pytest configuration
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Minimum version
minversion = 7.0

# Cache directory (relative to pytest.ini location)
cache_dir = .pytest_cache

# Add options
addopts = 
    -v
    --strict-markers
    --strict-config
    --tb=short
    --disable-warnings

# Markers
markers =
    asyncio: Async tests
    slow: Slow running tests

# Test discovery
norecursedirs = 
    .git
    .tox
    dist
    build
    *.egg
    __pycache__
    .pytest_cache

# Coverage
[coverage:run]
source = 
    app
data_file = .coverage
omit = 
    */tests/*
    */test_*
    */__pycache__/*
    */migrations/*
    */venv/*
    */.venv/*

[coverage:report]
fail_under = 80
html_dir = htmlcov
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING: 