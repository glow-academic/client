# requirements.txt

# FastAPI core framework, plus all the "standard" extras
# (email-validator, httpx TestClient, template support, form parsing, and Uvicorn)
fastapi[standard]

# Uvicorn ASGI server with its "standard" performance extras
# (uvloop, httptools, etc.)
uvicorn[standard]

python-dotenv

sqlmodel 

openai
openai-agents[litellm]==0.0.19

python-docx
matplotlib
numpy

# Add psycopg2-binary for SQLAlchemy compatibility
psycopg2-binary

# Keep psycopg for newer code if needed
psycopg[binary]

# Database
sqlacodegen

# Production server
gunicorn

# Development tools
ruff
mypy
respx

# Document processing dependencies
PyPDF2>=3.0.0
python-docx>=1.1.0

# Encryption/decryption
cryptography>=41.0.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
httpx>=0.24.0

# WebSocket support
python-socketio>=5.10.0
websockets>=12.0

# mcp
fastmcp

# RTC
aiortc

# webrtcvad
webrtcvad