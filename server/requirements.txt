# requirements.txt

# FastAPI core framework, plus all the "standard" extras
# (email-validator, httpx TestClient, template support, form parsing, and Uvicorn)
fastapi[standard]

# Uvicorn ASGI server with its "standard" performance extras
# (uvloop, httptools, etc.)
uvicorn[standard]

python-dotenv

sqlmodel 

openai
openai-agents[litellm]==0.0.19

python-docx
matplotlib
numpy

# Add psycopg2-binary for SQLAlchemy compatibility
psycopg2-binary

# Keep psycopg for newer code if needed
psycopg[binary]

# Production server
gunicorn

# Document processing
PyPDF2>=3.0.0
python-docx>=1.1.0

# WebSocket support
python-socketio>=5.10.0
websockets>=12.0

# Redis for Socket.IO clustering (optional)
redis>=5.0.0

# mcp
fastmcp

# RTC
aiortc
webrtcvad
soundfile

# Encryption / crypto
cryptography>=41.0.0